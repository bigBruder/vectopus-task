import { createSlice } from "@reduxjs/toolkit";

const initialState = {
  imgList: []
}

export const imgList = createSlice({
  name: 'imgList',
  initialState,
  reducers: {},
  extraReducers: {
    
  }
})